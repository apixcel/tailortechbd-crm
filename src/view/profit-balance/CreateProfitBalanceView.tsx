"use client";
import ProfitBalanceForm from "@/components/profit-balance/ProfitBalanceForm";
const CreateProfitBalanceView = () => {
  return (
    <div>
      <ProfitBalanceForm isLoading={false} onSubmit={() => {}} />
    </div>
  );
};

export default CreateProfitBalanceView;
